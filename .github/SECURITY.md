# Security policy

Report vulnerabilities privately through GitHub's security reporting when available.

Do not include API keys, private prompts, user documents, model-provider credentials or private vector-store content in issues.

Cloud9 Embedding Engine does not require an API key by default. If you expose the endpoint beyond a trusted network, place it behind authentication and TLS.
